<b>git configurations and settings</b>
<p>
create symlink for vimrc, where the repo is in ~/.vim:
<br>
\t> ln -s ~/.vim/vimrc ~/.vimrc 
<p>
get submodules
<br>
\t> git submodule init
\t> git submodule update
